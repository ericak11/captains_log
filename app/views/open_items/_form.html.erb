<%= form_for(@open_item) do |f| %>
  <% if @open_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@open_item.errors.count, "error") %> prohibited this open_item from being saved:</h2>

      <ul>
      <% @open_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :task %><br>
    <%= f.select :task , Expense::TYPES %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_field :date %>
  </div>
  <div class="field">
    <%= f.label :assaigned_to %><br>
    <%= f.collection_select :user_id, @crew, :id, :name, include_blank: true %>
  </div>
  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_field :notes %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
